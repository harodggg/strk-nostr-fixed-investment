apiVersion: v1
kind: ConfigMap
metadata:
  name: my-config
data:
  PRIVATE_KEY: "MyAwesomeApp"
  ADDRESS: "INFO"
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: starknet-restaking
spec:
  schedule: "0 * * * *" # 每天凌晨 0 点执行
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: starknet-restaking
              imagePullPolicy: Always
              image: axelharold/starknet-restaking:latest
              envFrom:
                - configMapRef:
                    name: my-config
          restartPolicy: OnFailure
